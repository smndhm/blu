name: Lint

on:
  pull_request:
    branches: [main]

jobs:
  setup:
    uses: ./.github/workflows/common.yml
  lint:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Run linting
        run: pnpm lint
